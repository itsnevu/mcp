{
  "//": "The web app. Renamed off 'bugglo' so the publishable workspace package (packages/bugglo) can own that name on npm. Nothing depends on this name — it is private and never published.",
  "name": "bugglo-web",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "next dev",
    "lint": "eslint .",
    "test": "vitest run",
    "verify": "npm run lint && npm test && npm run build",
    "build": "next build",
    "start": "next start"
  },
  "//overrides": "postcss <=8.5.17 is GHSA-r28c-9q8g-f849 (sourceMappingURL path traversal), so the floor here must stay ABOVE 8.5.17 — a ^8.5.17 range still resolves to the vulnerable version and silently reopens it.",
  "overrides": {
    "postcss": "^8.5.23",
    "sharp": "^0.35.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "bugglo": "*",
    "jose": "^6.2.3",
    "next": "^16.2.12",
    "openai": "^6.46.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "robinx-mcp": "^0.2.0",
    "viem": "^2.55.1"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "eslint-config-next": "^16.2.10",
    "vitest": "^4.1.10"
  }
}
